"""
model.lib — Home for pure I/O modules that hold no classes
------------------------------------------------------------
db_path / db_backup / task_db / settings_db / csv_io. No tkinter dependency,
functions only. __init__.py re-exports nothing (avoids circular imports).
"""
